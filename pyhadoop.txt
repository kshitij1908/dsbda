#WMapper.py

#!/usr/bin/env python3
import sys

for line in sys.stdin:
    # Remove leading/trailing whitespace and split on any whitespace
    words = line.strip().split()
   
    for word in words:
        # Emit each word with count 1
        print(f"{word}\t1")

#WReducer.py

#!/usr/bin/env python3
import sys

current_word = None
current_count = 0

for line in sys.stdin:
    line = line.strip()
   
    if not line:
        continue

    word, count = line.split("\t")
    count = int(count)

    if current_word == word:
        current_count += count
    else:
        if current_word is not None:
            print(f"{current_word}\t{current_count}")
        current_word = word
        current_count = count

# Output the last word
if current_word is not None:
    print(f"{current_word}\t{current_count}")


hdfs dfs -mkdir /3095
hdfs dfs -copyFromLocal /home/hduser/input.txt /3095
hadoop jar /usr/local/hadoop/share/hadoop/tools/lib/hadoop-streaming-3.3.6.jar -input /3095/input.txt -output /output_final -mapper 'python3 PMapper.py' -reducer 'python 3 PReducer.py'

hadoop jar /home/kshitijdixit/hadoop/share/hadoop/tools/lib/hadoop-streaming-3.3.6.jar \
-input /3095/input.txt \
-output /output_final \
-mapper "python3 PMapper.py" \
-reducer "python3 PReducer.py"

hdfs dfs -cat /output_final/part-00000
